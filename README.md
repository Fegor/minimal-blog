# 极简博客系统

一个基于 React + GitHub Pages 的个人博客系统，支持快速记录、分类管理和日夜模式切换。

## 功能特性

- 🚀 **快速输入**: 打开网页即可输入内容，像微博一样便捷
- 📝 **Markdown 支持**: 完整的 Markdown 编辑和渲染，支持图片、代码高亮、自动编号列表
- 🎨 **日夜模式**: 精心设计的浅色/深色主题，自动保存偏好
- 📂 **分类管理**: 日记、技术、生活经验三大分类
- 🔐 **安全认证**: Cloudflare Workers 保护编辑权限
- 📱 **响应式设计**: 完美适配桌面和移动设备

## 技术栈

- **前端框架**: React 18 + Vite
- **样式方案**: CSS Modules + CSS Variables
- **Markdown**: react-markdown + remark-gfm + rehype-highlight
- **认证层**: Cloudflare Workers
- **存储**: GitHub Repository
- **部署**: GitHub Pages

## 项目结构

```
minimal-blog/
├── frontend/                 # React 前端应用
│   ├── src/
│   │   ├── components/      # 组件
│   │   │   ├── QuickInput.jsx      # 快速输入框
│   │   │   ├── PostList.jsx        # 文章列表
│   │   │   ├── PostEditor.jsx      # 完整编辑器
│   │   │   ├── Sidebar.jsx         # 侧边栏导航
│   │   │   └── ThemeToggle.jsx     # 主题切换
│   │   ├── pages/           # 页面
│   │   ├── services/        # API 服务
│   │   ├── styles/          # 样式文件
│   │   ├── utils/           # 工具函数
│   │   ├── App.jsx
│   │   └── main.jsx
│   ├── public/
│   ├── package.json
│   └── vite.config.js
├── cloudflare-worker/       # Cloudflare Workers 代码
│   └── worker.js
├── posts/                   # 文章内容（GitHub 仓库）
│   ├── diary/              # 日记分类
│   ├── tech/               # 技术分类
│   └── life/               # 生活经验分类
└── images/                  # 图片资源
```

## 安装部署

### 1. 克隆仓库

```bash
git clone https://github.com/yourusername/minimal-blog.git
cd minimal-blog
```

### 2. 安装依赖

```bash
cd frontend
npm install
```

### 3. 配置环境变量

创建 `.env` 文件：

```env
VITE_GITHUB_REPO=yourusername/minimal-blog
VITE_CLOUDFLARE_WORKER_URL=https://your-worker.your-subdomain.workers.dev
```

### 4. 本地开发

```bash
npm run dev
```

### 5. 构建部署

```bash
npm run build
```

将 `dist` 目录部署到 GitHub Pages。

## Cloudflare Workers 配置

### 1. 创建 Worker

在 Cloudflare Dashboard 创建新的 Worker。

### 2. 配置环境变量

在 Worker 设置中添加：

- `GITHUB_TOKEN`: GitHub Personal Access Token (需要 repo 权限)
- `AUTH_SECRET`: JWT 签名密钥
- `ALLOWED_EMAILS`: 允许登录的邮箱列表（逗号分隔）

### 3. 部署 Worker 代码

将 `cloudflare-worker/worker.js` 的内容复制到 Worker 编辑器中并部署。

## GitHub 设置

### 1. 创建 Personal Access Token

访问 GitHub Settings → Developer settings → Personal access tokens

权限需求：
- `repo` (完整权限)
- `workflow` (如果使用 GitHub Actions)

### 2. 启用 GitHub Pages

在仓库设置中启用 GitHub Pages，选择部署分支（通常是 `gh-pages`）。

## 使用说明

### 快速记录

1. 打开博客首页
2. 在顶部快速输入框中输入标题
3. 点击展开，输入 Markdown 内容
4. 选择分类（日记/技术/生活经验）
5. 点击发布，自动保存到 GitHub

### 编辑文章

1. 点击文章旁边的编辑按钮
2. 在编辑器中修改内容
3. 保存后自动更新到 GitHub

### 插入图片

1. 在编辑器中点击图片上传按钮
2. 选择本地图片
3. 自动上传到 GitHub 仓库的 images 目录
4. 自动插入 Markdown 引用链接

### 切换主题

点击右上角的主题切换按钮，在日间/夜间模式之间切换。

## 文章格式

每篇文章使用以下 YAML Front Matter 格式：

```markdown
---
title: 文章标题
date: 2025-02-08
category: tech
tags: [React, JavaScript]
---

这里是文章正文内容...
```

## 开发路线图

- [x] 基础架构设计
- [ ] 快速输入组件
- [ ] Markdown 编辑器集成
- [ ] GitHub API 集成
- [ ] Cloudflare Workers 认证
- [ ] 日夜模式切换
- [ ] 图片上传功能
- [ ] 移动端优化
- [ ] 搜索功能
- [ ] 标签系统

## 许可证

MIT License
